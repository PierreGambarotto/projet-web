<h3>Comments</h3>
<% @post.comments.each do |comment| %>
<fieldset>
	<legend>
    <%= comment.author %>
	</legend>
	<p><%=# link_to 'Delete Comment', [comment.post, comment],
         #      :confirm => 'Are you sure?',
         #      :method => :delete 
			%>
	</p>
   <p><%= comment.body %></p>
</fieldset>
<% end %>

